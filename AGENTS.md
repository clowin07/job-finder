[RULES-OK]
# Agent Instructions

## Scope

- 충남대학교 에너지공학과 1학년 학생을 위한 인턴·신입 채용공고 추천 화면을 유지한다.
- 인턴과 신입을 함께 보여주고 어느 한쪽을 기본 우선하지 않는다.
- 프로젝트 API와 `.env` 값은 사용하거나 노출하지 않는다.

## Run and verify

| 목적 | 명령 |
|---|---|
| 로컬 서버 실행 | `node server.cjs` |
| JavaScript 문법 확인 | `node --check script.js` |
| 화면 확인 | `http://127.0.0.1:4173/` 열기 |

## Recommendation rules

- 연구·실험·품질 분야 공고를 추천 목록의 위쪽에 배치한다.
- 신입·무경력 조건이고 마감일까지 7일 이상 남은 공고만 추천 대상에 포함한다.
- 재학생·졸업예정자·졸업자 지원 가능 공고를 모두 포함한다.
- 지역·기업 유형·근무 방식·전공·급여 조건으로 제한하지 않는다.
- 위 규칙 외의 정렬 기준과 추천 점수 계산 방식은 만들지 않는다.
- 세부 학년 제한은 별도 답변이 없으므로 추정하지 않는다.

## UI requirements

- 공고를 빠르게 훑도록 카드에서 직무명과 기업명을 가장 먼저 보인다.
- 카드에 채용 형태·업무 분야·근무 지역·지원 자격·경력·전공·급여·마감일을 표시한다.
- 왼쪽 상세 필터 패널을 고정하고 `인턴`·`신입`·`전체` 필터를 제공한다.
- `연구·실험·품질`, 지역, 기업 유형, 근무 방식, 지원 자격 필터를 제공한다.
- 상세 화면의 제목 옆 상단에 `지원하기` 버튼을 두고 카드에 숫자 추천도를 표시한다.
- `저장`을 누르면 저장한 공고만 모아 보고 카드 선택 시 상세 내용을 보인다.
- 세련되고 전문적인 분위기, 블랙·화이트 중심 색상, 카드·배지·큰 이미지, 전문적인 문구를 사용한다.

## References

| 용도 | 파일 |
|---|---|
| API 계약 참고만 | `채용정보조회api.txt` |
| 코드 정의 참고만 | `MOEF_NKOD_DB_05_코드 정의서_v1.2 [배포용].pdf` |

## Out of scope

- API 연결, 실제 지원서 제출, 미정 정렬 기준, 추천 점수 공식은 구현하지 않는다.
- 표시 공고 수, 글꼴, 모서리, 애니메이션, 모바일 동작은 결정하지 않는다.
